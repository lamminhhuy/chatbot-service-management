export enum MediaType {
    IMAGE = 'image',
    VIDEO = 'video',
    DOCUMENT = 'document',
    AUDIO = 'audio'
}

export enum MediaReferenceType {
    POST = 'post',
    COMMENT = 'comment',
    USER = 'user'
}
