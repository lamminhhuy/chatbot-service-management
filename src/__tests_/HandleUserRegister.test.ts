import { describe, it, beforeEach, expect, vi, Mock } from "vitest";

describe(('handle user register') => {

    beforeEach(()=>{

    })

    it('it should successfully register a user', ()=> {

    })
    
    it('it should return message when a user enter an existing email', ()=> {

    })

    
})